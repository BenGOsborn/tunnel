#include "json/JSON.hpp"

namespace json
{
    std::expected<Object, std::string> JSON::FromString(const std::string &str)
    {
    }

    std::expected<std::string, std::string> JSON::ToString(const Object &obj)
    {
    }
}